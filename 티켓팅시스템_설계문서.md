# 실시간 콘서트 예매 시스템 — 프로젝트 설계 문서

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 도메인 | 콘서트 예매 (뮤지컬/스포츠 등 제외, 콘서트로 한정) |
| 좌석 규모 | 100석 |
| 결제 처리 | Mock 처리 (실제 PG 미연동, 추후 토스페이먼츠 API 연동 고려) |
| 핵심 기술 챌린지 | ① 실시간 처리/동시성 ② 대용량 데이터 처리/배치 ③ 외부 API 연동(AI/LLM) |

## 2. 인증/인가 범위

- 인증 방식: JWT (Access Token + Refresh Token)
- 역할 구분: `ADMIN`(공연/좌석 관리), `USER`(조회/예매)
- 회원가입: 이메일/비밀번호 자체 가입만 (소셜 로그인 제외)
- 비회원 예매: 불가 (회원만 예매 가능)

## 3. 핵심 엔티티 설계

### User (회원)
| 필드 | 타입 | 설명 |
|---|---|---|
| id | PK | |
| email | string | unique |
| password | string | 해시 저장 |
| role | enum | ADMIN / USER |

### Concert (공연)
| 필드 | 타입 | 설명 |
|---|---|---|
| id | PK | |
| title | string | 공연명 |
| venue | string | 공연장 |
| performanceDate | datetime | 공연 일시 |
| totalSeats | int | 100 고정 |
| status | enum | 예매전 / 예매중 / 예매마감 |

### Seat (좌석) — 동시성 처리의 핵심
| 필드 | 타입 | 설명 |
|---|---|---|
| id | PK | |
| concertId | FK | |
| seatNumber | string | 예: A-1 ~ A-100 |
| status | enum | AVAILABLE / HOLDING / SOLD |
| heldBy | FK(User), nullable | 선점 중인 사용자 |
| heldAt | datetime, nullable | 선점 시각 (Redis TTL과 별개로 감사 로그용) |

### Reservation (예매)
| 필드 | 타입 | 설명 |
|---|---|---|
| id | PK | |
| userId | FK | |
| concertId | FK | |
| seatId | FK | |
| status | enum | PENDING / CONFIRMED / CANCELLED |
| createdAt | datetime | |

### Payment (결제, Mock)
| 필드 | 타입 | 설명 |
|---|---|---|
| id | PK | |
| reservationId | FK | |
| amount | int | |
| status | enum | PENDING / SUCCESS / FAILED |
| paidAt | datetime, nullable | |

## 4. 좌석 선점 동시성 전략

**선택한 방식: Redis TTL 기반 선점**

- 좌석 선점 요청 시 `SETNX seat:{seatId} {userId}` + `EXPIRE 300` (5분)
- SETNX 성공 → DB Seat 상태를 HOLDING으로 변경
- SETNX 실패 (이미 선점됨) → 409 Conflict 응답
- 결제 성공 → Redis 키 삭제 + DB Seat 상태 SOLD, Reservation CONFIRMED
- TTL 만료 (5분 내 결제 없음) → Redis 키 자동 삭제, DB 상태는 별도 동기화 필요 (핵심 트러블슈팅 포인트)

**검토했던 대안**: DB 컬럼(`heldAt`) + 스케줄러 방식 → 만료 처리에 최대 1분 오차 발생, 동시 요청 시 DB 락 경합이 그대로 노출되는 단점으로 기각.

**남은 문제 (구현하며 직접 풀어볼 부분)**: Redis 키는 만료됐는데 DB의 Seat 상태가 여전히 HOLDING으로 남아있는 정합성 불일치. 조회 시점에 Redis 존재 여부로 실시간 보정하는 방식 등을 직접 설계해볼 것.

## 5. API 명세

### 인증
| Method | Endpoint | 설명 | 권한 |
|---|---|---|---|
| POST | /auth/signup | 회원가입 | 없음 |
| POST | /auth/login | 로그인, 토큰 발급 | 없음 |
| POST | /auth/refresh | Access Token 재발급 | Refresh Token |

### 공연
| Method | Endpoint | 설명 | 권한 |
|---|---|---|---|
| POST | /concerts | 공연 등록 (좌석 100석 자동 생성) | ADMIN |
| GET | /concerts | 공연 목록 조회 | USER/ADMIN |
| GET | /concerts/:id | 공연 상세 조회 | USER/ADMIN |

### 좌석
| Method | Endpoint | 설명 | 권한 |
|---|---|---|---|
| GET | /concerts/:id/seats | 좌석 배치/상태 조회 | USER |
| POST | /seats/:id/hold | 좌석 선점 | USER |
| DELETE | /seats/:id/hold | 선점 취소 | USER (본인 건) |

### 예매
| Method | Endpoint | 설명 | 권한 |
|---|---|---|---|
| POST | /reservations | 예매 생성 (PENDING) | USER |
| POST | /reservations/:id/confirm | 결제 완료 후 확정 | USER (본인 건) |
| GET | /reservations/my | 내 예매 내역 | USER |
| POST | /reservations/:id/cancel | 예매 취소 | USER (본인 건) |

### 결제 (Mock)
| Method | Endpoint | 설명 | 권한 |
|---|---|---|---|
| POST | /payments | Mock 결제 처리 | USER |
| GET | /payments/:reservationId | 결제 상태 조회 | USER (본인 건) |

## 6. 핵심 플로우

```
GET /concerts/:id/seats
  → POST /seats/:id/hold (Redis SETNX + DB HOLDING)
  → POST /reservations (PENDING 생성)
  → POST /payments (Mock 결제)
  → POST /reservations/:id/confirm (Redis 삭제 + DB SOLD/CONFIRMED)
```

## 7. 향후 확장 예정 (기본 흐름 완성 후)

- 대기열 시스템 (접속 폭주 시 순서 제어)
- 정산 배치 (새벽 배치로 판매자 정산 처리, 인기 공연 랭킹 집계)
- 리뷰 요약 API (LLM 기반)
- 부정 예매(매크로) 탐지 로직
- 실제 PG(토스페이먼츠) 연동

## 8. 개발 진행 순서

1. 기본 CRUD 먼저 완성 (AI에게 완성본 요청): 회원가입/로그인, 공연 등록/조회
2. 핵심 챌린지 부분은 의도적으로 미완성 버전으로 시작 (동시성 처리 없이 좌석 상태만 변경하는 단순 버전) → 직접 동시 요청 테스트로 문제 재현 → 원인 분석 후 Redis 기반으로 개선
3. 정산 배치, 랭킹 집계 등에서 발생하는 N+1/성능 문제도 동일한 방식으로 직접 겪고 해결
4. 트러블슈팅 기록은 기존 프롬프트 템플릿의 8단계 양식(문제/첫 시도/원인 분석/최종 해결/성과)으로 정리
