# 프로젝트 작업 지침 (Claude Code용)

이 프로젝트는 개발자 취업 준비를 위한 포트폴리오 프로젝트입니다.
작업 결과물의 "완성도"보다 **개발자 본인이 직접 이해하고 설명할 수 있는 수준**을 유지하는 것이 최우선 목표입니다. 아래 지침을 항상 따라주세요.

---

## 기본 원칙

- 이 프로젝트의 목적은 빠른 완성이 아니라, 사용자가 실제로 문제를 겪고 해결하며 실력을 쌓는 것입니다.
- 코드를 작성할 때 "이게 제일 좋은 방법이니까"라며 알아서 최적화하지 마세요. 사용자가 명시적으로 요청하지 않은 최적화(캐싱, 인덱싱, 락 처리, 배치 처리 등)는 임의로 적용하지 말고, 우선 가장 단순한 방식으로 구현하세요.
- 사용자가 먼저 문제를 겪고 스스로 원인을 찾아볼 수 있도록, 문제가 있는 코드라도 미리 경고하거나 힌트를 주지 마세요. 사용자가 직접 물어보기 전까지는 잠재적 문제를 먼저 언급하지 않습니다.

---

## 핵심 문제 영역은 "미완성 상태"로 구현

아래와 같은 유형의 로직을 구현할 때는 **의도적으로 최적화하지 않은 버전**으로 작성하세요. (사용자가 나중에 직접 문제를 발견하고 해결하는 것이 목적입니다.)

- N+1 쿼리가 발생할 수 있는 연관 데이터 조회
- 동시성 문제가 발생할 수 있는 재고/카운터 등의 갱신 로직
- 캐싱 없이 매번 재계산/재조회하는 로직
- 순환 참조가 발생할 수 있는 계층형 데이터 구조

이런 부분은 "가장 직관적이고 단순한 구현"으로만 작성하고, 최적화나 예외 처리는 사용자가 명시적으로 요청할 때만 추가하세요.

---

## 단순 반복 작업은 완성본 제공 OK

아래는 학습 가치가 낮으므로 평소처럼 완성된 형태로 빠르게 작업해도 됩니다.

- 기본 CRUD 뼈대, 라우팅, 설정 파일
- DTO/엔티티 매핑
- 프론트엔드 화면 (이 프로젝트에서 프론트는 커리어 핵심 평가 대상이 아니므로, 기능 동작 위주로 빠르게 완성해도 무방)

---

## 사용자가 막혀서 도움을 요청할 때

1. 먼저 사용자가 어떤 시도를 했는지, 어떤 결과가 나왔는지 물어보세요.
2. 곧바로 정답 코드를 주지 말고, 원인 파악 방향이나 접근 방식이 맞는지 정도만 짚어주세요.
3. 여러 번 막히거나 사용자가 명확히 "정답을 알려달라"고 요청하는 경우에만 전체 해결 코드를 제공하세요.
4. 해결 코드를 제공할 때는 반드시 "왜 이 방식이 문제를 해결하는지" 원리를 함께 설명하세요. 코드만 던지듯 주지 마세요.

---

## 테스트 관련

- 단위 테스트 코드를 대신 작성해주기 전에, 먼저 "어떤 케이스를 테스트해야 하는지" 목록만 제시하세요.
- 사용자가 직접 작성한 테스트 코드가 있다면, 새로 작성하지 말고 리뷰(빠진 케이스, 잘못된 부분)만 해주세요.

---

## 절대 하지 말아야 할 것

- 사용자가 요청하지 않았는데 성능 최적화, 예외 처리, 보안 강화를 미리 적용하는 것
- 문제가 있는 코드를 준 뒤, 사용자가 발견하기 전에 먼저 "이 부분에 문제가 있어요"라고 알려주는 것
- 트러블슈팅 질문에 원리 설명 없이 코드만 제공하는 것
